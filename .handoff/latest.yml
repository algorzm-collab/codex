mission: "ALOS Mission Control dashboard viability and read-only GitHub evidence sync"
current_objective: "Hand off the uncommitted dashboard/evidence-sync implementation to 5.3 Spark so it can review, finish, commit, and push safely."
status: "HANDOFF"
progress_estimate: "75%"

current_context:
  issue: "No single issue assigned in this thread; work follows CEO instruction to continue the next dashboard step."
  branch: "codex/dashboard-viability-skill-capture"
  pull_request: ""
  last_commit: "e8a8a10 mission-control: add Codex dispatch layer correction"

completed_work:
  - "Recovered the static Mission Control dashboard from mojibake Korean text."
  - "Added decision and failure-lessons fields to the dashboard surface."
  - "Created the dashboard-viability-review skill to capture failure factors and stop/simplify/continue judgment."
  - "Extended SiteExporter so exported dashboard data preserves decision and failure_lessons."
  - "Added GitHub evidence normalization for saved evidence JSON and GitHub issue-list style JSON."
  - "Added CLI entrypoint: python alos_cli.py dashboard-from-issues issues.json --out site/data.json."
  - "Verified the legacy positional CLI call still works: python alos_cli.py Test \"Do work\"."

modified_files:
  - path: "alos_cli.py"
    reason: "Adds dashboard-from-issues CLI command while preserving existing positional mission CLI."
  - path: "alos/github_evidence.py"
    reason: "New read-only evidence adapter from saved GitHub issue/evidence JSON to dashboard plan."
  - path: "alos/site_exporter.py"
    reason: "Preserves decision and failure_lessons in site/data.json payload."
  - path: "checks/smoke_alos_cli_dashboard.py"
    reason: "Verifies the new CLI command writes dashboard data."
  - path: "checks/smoke_github_evidence.py"
    reason: "Verifies saved evidence JSON becomes dashboard data."
  - path: "checks/smoke_github_issue_evidence.py"
    reason: "Verifies GitHub issue-list style JSON becomes evidence and dashboard data."
  - path: "checks/smoke_site_exporter.py"
    reason: "Verifies SiteExporter keeps decision, failure_lessons, and next_prompts."
  - path: "site/index.html"
    reason: "Restores Korean UI labels and adds decision/failure-lessons sections."
  - path: "site/app.js"
    reason: "Renders decision and failure lessons from dashboard data."
  - path: "site/data.json"
    reason: "Static read-only cockpit payload with continue/simplify decision and lessons."
  - path: "site/cockpit.txt"
    reason: "Documents cockpit rule: GitHub remains source of truth."
  - path: "site/style.css"
    reason: "Styles failure-lessons list."
  - path: "skills/dashboard-viability-review/SKILL.md"
    reason: "Reusable skill for judging whether dashboard work should continue, simplify, or stop."
  - path: "skills/dashboard-viability-review/agents/openai.yaml"
    reason: "Skill UI metadata generated by skill-creator."
  - path: ".handoff/latest.yml"
    reason: "This handoff package."

important_decisions:
  - decision: "Continue dashboard work, but simplify."
    reason: "The dashboard is useful as a read-only Mission Control cockpit, but live remote control is premature."
  - decision: "GitHub remains the source of truth."
    reason: "Issues, PRs, commits, and comments preserve state better than a dashboard UI."
  - decision: "Build read-only sync before approval/control."
    reason: "Remote approval without durable evidence, audit trail, and rollback would increase risk."
  - decision: "Use saved JSON input before live API calls."
    reason: "It allows deterministic tests without network/auth risk."

rejected_options:
  - option: "Build live remote-control dashboard now."
    reason: "Too much auth, permission, and audit risk before read-only evidence sync is proven."
  - option: "Make dashboard the primary memory."
    reason: "This would duplicate GitHub state and increase CEO update burden."
  - option: "Add new libraries."
    reason: "User explicitly forbade new libraries, and current scope does not require them."

risks:
  - "Changes are uncommitted and unpushed."
  - "GitHub live retrieval is not implemented yet; current path consumes saved issue/evidence JSON."
  - "Existing smoke_alos.py and smoke_briefing.py require PYTHONPATH=. when run directly."
  - "Need careful review before commit because this branch contains cumulative dashboard, skill, exporter, evidence, and CLI changes."

blockers:
  - "No blocker for local continuation."
  - "Network/auth approval will be required if the next agent chooses to call gh or GitHub API directly."

validation_run:
  passed:
    - "python checks\\smoke_alos_cli_dashboard.py"
    - "python alos_cli.py Test \"Do work\""
    - "python checks\\smoke_github_issue_evidence.py"
    - "python checks\\smoke_github_evidence.py"
    - "python checks\\smoke_site_exporter.py"
    - "python checks\\smoke_site.py"
    - "python -m json.tool site\\data.json"
    - "node --check site\\app.js"
    - "python C:\\Users\\USER\\.codex\\skills\\.system\\skill-creator\\scripts\\quick_validate.py skills\\dashboard-viability-review"
    - "$env:PYTHONPATH='.'; python checks\\smoke_alos.py"
    - "$env:PYTHONPATH='.'; python checks\\smoke_briefing.py"
  notes:
    - "Direct smoke_alos.py and smoke_briefing.py fail without PYTHONPATH=. due pre-existing import path behavior."

next_recommended_action:
  - "Review git diff for the cumulative dashboard/evidence-sync branch."
  - "Run the validation list above."
  - "If acceptable, stage only the listed modified_files, commit with a focused message, and push branch codex/dashboard-viability-skill-capture."
  - "Then optionally create/update a GitHub issue or PR comment with the result."
  - "Next implementation after commit: connect actual gh issue list --json or GitHub connector output to the saved issue JSON input."

recommended_next_agent: "5.3 Spark"

approval_needed:
  required: false
  reason: "Local review and commit can proceed without new product/auth/schema/payment changes. Network push or live GitHub fetch may need approval."
  approval_owner: "none"

continuity_notes: "Keep CEO-facing reports short. Always include one-line next work with expected token cost, expected effect, and why it matters. Do not add live approval/control until read-only GitHub sync is reliable."
